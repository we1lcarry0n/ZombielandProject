using System;
using Zombieland.GameScene0.RootModule;

namespace Zombieland.GameScene0.GameManagerModule
{
    public class GameManager // : IController, IGameManager
    {
        /*public bool IsReady;
        public event Action<string, IController> OnReady;
        public ISubsystemsActivator SubsystemsActivator;

        private IRootController _parentController;
        
        
        public void Initialize(IController parentController)
        {
            _parentController = parentController as IRootController;

            SubsystemsActivator.OnReady += OnSystemReadyHandler;
            SubsystemsActivator.SetSubsystemsActivity(true);
        }

        public void Disable()
        {
            throw new NotImplementedException();
        }

        public void OnSystemReadyHandler(string errorMessage)
        {
            SubsystemsActivator.OnReady += OnSystemReadyHandler;
        }*/
    }
}